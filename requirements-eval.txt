# Eval / local tooling (for eval_local.py, etc.)
# 本文件用于本地评测工具链；不参与最终评测镜像。

python-docx>=1.1.0
jieba>=0.42.1
rouge-score>=0.1.2
tqdm>=4.66.0
requests>=2.31.0

# Optional: for token counting in eval_local.py
transformers>=4.39.0
