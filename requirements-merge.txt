# 工具类前置（本地用，不参与最终评测镜像）
# Minimal deps for running merge_adapter.py locally (e.g. on WSL)

# modelscope>=1.15.0
# transformers>=4.39.0
# accelerate>=0.27.0
# safetensors>=0.4.2
# sentencepiece>=0.1.99
# peft>=0.10.0

# Eval / local tooling (for eval_local.py, etc.)
python-docx>=1.1.0
jieba>=0.42.1
rouge-score>=0.1.2
tqdm>=4.66.0
requests>=2.31.0

# NOTE:
# torch is intentionally not pinned here because the correct wheel depends on
# your environment (CPU vs CUDA). For CPU-only on Linux, prefer:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
