# Merge tooling (for merge_adapter.py)
# 本文件用于本地融合/导出工具链；不参与最终评测镜像。

# NOTE:
# torch is intentionally not pinned here because the correct wheel depends on
# your environment (CPU vs CUDA). For CPU-only on Linux, prefer:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

modelscope>=1.15.0
transformers>=4.39.0
accelerate>=0.27.0
safetensors>=0.4.2
sentencepiece>=0.1.99
peft>=0.10.0
